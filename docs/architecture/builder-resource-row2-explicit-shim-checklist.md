# Builder Resource Row 2 Explicit-Shim Checklist

## Purpose

Use this checklist only for row `2` of:

- `BUILDER_RESOURCE_OWNERSHIP_CLOSURE`

This checklist now serves as regression-only support.

## Row

- row `2`
- `framework contract metadata resource`

## Checklist

- check whether a later docs set names one temporary reason for keeping a root framework metadata copy
- check whether that same docs set says the root metadata copy is explicitly temporary
- check whether that same docs set names one explicit removal trigger
- if all three appear together, reopen row `2` for explicit-shim review
- otherwise keep row `2` on the resolved `DELETE_NOW` path
